# v0.2.3 — OSS cleanup & CI fixes

**Full changelog:** [CHANGELOG.md](../../CHANGELOG.md)

## Highlights

- Topology smoke peers align with `demo-*` service names (no `xt-*` host literals).
- GitHub CI: `dart pub get --no-example`, `dart analyze lib bin test`, OAP **10.1.0** smoke, `actions/checkout@v5`.
- Contributor docs: [CONTRIBUTING.md](../../CONTRIBUTING.md); OAP 10.2+ storage guidance in [OAP_SETUP.md](../oap/OAP_SETUP.md).

## Install

```yaml
dependencies:
  skywalking_dart:
    git:
      url: https://github.com/songzhendong/skywalking-dart.git
      ref: v0.2.3
```

## Verify

```bash
export SW_AGENT_COLLECTOR_BACKEND_SERVICES=127.0.0.1:11800
dart run bin/verify_native.dart --quick
```
